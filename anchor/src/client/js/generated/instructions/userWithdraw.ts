/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { PRISMPAPERSDAPP_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const USER_WITHDRAW_DISCRIMINATOR = new Uint8Array([
  53, 254, 26, 242, 119, 237, 73, 33,
]);

export function getUserWithdrawDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    USER_WITHDRAW_DISCRIMINATOR
  );
}

export type UserWithdrawInstruction<
  TProgram extends string = typeof PRISMPAPERSDAPP_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UserWithdrawInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type UserWithdrawInstructionDataArgs = { amount: number | bigint };

export function getUserWithdrawInstructionDataEncoder(): FixedSizeEncoder<UserWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: USER_WITHDRAW_DISCRIMINATOR })
  );
}

export function getUserWithdrawInstructionDataDecoder(): FixedSizeDecoder<UserWithdrawInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getUserWithdrawInstructionDataCodec(): FixedSizeCodec<
  UserWithdrawInstructionDataArgs,
  UserWithdrawInstructionData
> {
  return combineCodec(
    getUserWithdrawInstructionDataEncoder(),
    getUserWithdrawInstructionDataDecoder()
  );
}

export type UserWithdrawAsyncInput<
  TAccountUser extends string = string,
  TAccountUserVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userVault?: Address<TAccountUserVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: UserWithdrawInstructionDataArgs['amount'];
};

export async function getUserWithdrawInstructionAsync<
  TAccountUser extends string,
  TAccountUserVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRISMPAPERSDAPP_PROGRAM_ADDRESS,
>(
  input: UserWithdrawAsyncInput<
    TAccountUser,
    TAccountUserVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UserWithdrawInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PRISMPAPERSDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userVault: { value: input.userVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userVault.value) {
    accounts.userVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 97, 117, 108, 116, 95, 117, 115, 101, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUserWithdrawInstructionDataEncoder().encode(
      args as UserWithdrawInstructionDataArgs
    ),
    programAddress,
  } as UserWithdrawInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserVault,
    TAccountSystemProgram
  >);
}

export type UserWithdrawInput<
  TAccountUser extends string = string,
  TAccountUserVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userVault: Address<TAccountUserVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: UserWithdrawInstructionDataArgs['amount'];
};

export function getUserWithdrawInstruction<
  TAccountUser extends string,
  TAccountUserVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRISMPAPERSDAPP_PROGRAM_ADDRESS,
>(
  input: UserWithdrawInput<
    TAccountUser,
    TAccountUserVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UserWithdrawInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PRISMPAPERSDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userVault: { value: input.userVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUserWithdrawInstructionDataEncoder().encode(
      args as UserWithdrawInstructionDataArgs
    ),
    programAddress,
  } as UserWithdrawInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserVault,
    TAccountSystemProgram
  >);
}

export type ParsedUserWithdrawInstruction<
  TProgram extends string = typeof PRISMPAPERSDAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userVault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: UserWithdrawInstructionData;
};

export function parseUserWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUserWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUserWithdrawInstructionDataDecoder().decode(instruction.data),
  };
}
